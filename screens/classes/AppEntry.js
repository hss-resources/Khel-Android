import {React } from "react";
import { View, Text } from "react-native";

export default AppEntry extends React.Component {
  render() {
    return (
      <View>
        <Text>Entry of App</Text>
      </View>
    );
  }
}
